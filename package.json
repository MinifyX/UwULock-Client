{
  "name": "uwulock",
  "version": "0.1.0-beta.1",
  "private": true,
  "license": "GPL-3.0-only",
  "packageManager": "pnpm@11.0.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "pnpm --filter @uwulock/desktop dev",
    "tauri": "pnpm --filter @uwulock/desktop tauri",
    "build": "pnpm --filter @uwulock/desktop build",
    "typecheck": "pnpm -r typecheck",
    "lint": "prettier --check . && node scripts/check-i18n.mjs",
    "format": "prettier --write .",
    "build:setup": "node scripts/build-setup.mjs",
    "release": "node scripts/release.mjs"
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}
